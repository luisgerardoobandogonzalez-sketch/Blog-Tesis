import { GenderTranslatePipe } from './gender-translate-pipe';

describe('GenderTranslatePipe', () => {
  it('create an instance', () => {
    const pipe = new GenderTranslatePipe();
    expect(pipe).toBeTruthy();
  });
});
